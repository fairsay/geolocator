The code will look for the data files in here.

Note however that on Unix, the MaxMind data files will also be found from the locations where MaxMind data is installed by default.

The country coordinates data is from the MaxMind site; originally from the CIA world factbook.